{
  "name": "bai-chan-web",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "@newrelic/browser-agent": {
      "fflate": "^0.8.3"
    }
  },
  "scripts": {
    "dev:preview": "NEXT_PUBLIC_SERVER_PORT=3101 npm run dev -w apps/web -- --port 3100 --webpack",
    "dev:preview:server": "PORT=3101 npm run dev -w apps/server"
  }
}
